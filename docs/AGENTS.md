<!-- codex-workspace-bootstrap:managed -->
# AGENTS.md

This directory often mixes editable notes or trackers with derived outputs.

## Default Rule

- Edit source `.md`, `.csv`, `.json`, or workbook inputs by default.
- Treat rendered HTML, PDF, email sidecars, and `rendered/` previews as generated artifacts unless the task explicitly requires a final manual touch-up.
